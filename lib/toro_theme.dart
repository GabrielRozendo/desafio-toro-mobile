import 'package:flutter/material.dart';

final ThemeData toroTheme = ThemeData(
    primaryColor: ToroColors._primaryValue,
    appBarTheme: AppBarTheme(color: ToroColors._primaryValue));

class ToroColors {
  static const _primaryValue = Color(0xFF00add2);
}
